
#to reload Magento completely:

drop the magento folder
drop in a new one
cd ~/Sites/magento
chmod 777 app/etc/
chmod 777 var/
chmod 777 media/


# then run through the installer:
http://magento.dan.janrain.com/magento/


## most recent enc_key:  0abc0df3753a32302bcb8d2803c2df0e



#just after submitting admin credentials:

click close on incoming message dialog box

click on Index Management
select all
submit

go to messages inbox
view 100 msgs per page
select all
action - mark as read
submit

System / Cache Management
select all
action: disable
submit


# dump out this as the latest fresh copy:
/Applications/MAMP/Library/bin/mysqldump --user=magento --password=magento magento > fresh_install.sql



